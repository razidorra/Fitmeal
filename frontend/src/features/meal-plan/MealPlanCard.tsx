import type { MealPlan } from '../../shared/types';
export function MealPlanCard({ plan }: { plan: MealPlan }) { return <><section className="targets">{Object.entries(plan.targets).map(([key, value]) => <div key={key}><strong>{value}{key === 'calories' ? '' : 'g'}</strong><span>{key}</span></div>)}</section><section className="meal-list">{plan.meals.map(meal => <article className="meal" key={meal.time}><span>{meal.time}</span><div><h3>{meal.title}</h3><p>{meal.ingredients}</p></div><strong>{meal.calories} kcal<br /><small>{meal.protein}g protein</small></strong></article>)}</section></>; }
